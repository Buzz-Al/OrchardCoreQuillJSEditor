trigger:
  batch: true
  branches:
    include:
      - develop
  tags:
    include:
      - '*'

variables:
  BuildConfiguration: Release

jobs:
- template: jobs/test.yml
- template: jobs/publish.yml
